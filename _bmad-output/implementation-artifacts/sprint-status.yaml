# generated: 2026-08-10
# last_updated: 2026-08-10 (story 1.9 criada)
# project: elite-dev-RockHub
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic ainda não começou
#   - in-progress: Epic em andamento
#   - done: Todas as stories do epic concluídas
#
# Epic Status Transitions:
#   - backlog → in-progress: automático quando a primeira story é criada (via create-story)
#   - in-progress → done: manual, quando todas as stories chegam a 'done'
#
# Story Status:
#   - backlog: story só existe no epics.md
#   - ready-for-dev: arquivo da story criado na pasta de implementação
#   - in-progress: em implementação
#   - review: pronta para code review
#   - done: concluída
#
# Retrospective Status:
#   - optional: pode ser feita, não é obrigatória
#   - done: retrospectiva realizada
#
# Action Item Status:
#   - open: assumido numa retrospectiva, ainda não tratado
#   - in-progress: em andamento
#   - done: concluído
#
# WORKFLOW NOTES:
# ===============
# - O epic vira 'in-progress' automaticamente quando a primeira story dele é criada
# - Stories podem correr em paralelo se houver capacidade
# - Neste projeto: uma branch por epic, um commit por story, code review ao fim de cada epic
# - Da 3.7 em diante: techspec por grupo de stories em docs/, sem arquivo de story.
#   Os READMEs deixam de ser escritos a cada commit e vão inteiros para a Epic 6 —
#   exceto README.md#o-que-não-está-pronto, que continua sendo escrito na hora

generated: 2026-08-10
last_updated: 2026-08-13 (Stories 5.4 e 5.6 implementadas; Epic 5 fechada; suíte em 506)
project: elite-dev-RockHub
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ---------- Epic 1: Fundação, acesso e primeiro deploy ----------
  # Code review da epic inteira em 2026-08-11 (bmad-code-review, 3 camadas):
  # nenhum achado bloqueante. As correções foram aplicadas e a suíte passou de
  # 85 para 87 testes. Fechada em 'done' com o merge da branch na main (abf80d6).
  epic-1: done
  1-1-esqueleto-do-backend-que-responde: done
  1-2-esqueleto-do-frontend-com-a-identidade-aplicada: done
  1-3-modelo-de-usuario-e-primeira-migracao: done
  1-4-entrar-com-e-mail-e-senha: done
  1-5-cadastro-de-cliente: done
  1-6-cada-papel-so-acessa-o-que-lhe-cabe: done
  1-7-dados-semeados-para-avaliacao: done
  1-8-backend-e-banco-no-ar-na-railway: done
  1-9-frontend-no-ar-na-vercel: done
  epic-1-retrospective: optional

  # ---------- Epic 2: Publicação de eventos pelo organizador ----------
  # Fora da numeração: filtro de classificação na Discovery (segmentId=Music
  # sempre, genreId=Rock só na vitrine sem termo). Commit `feat` avulso, spec em
  # docs/techspec-filtro-do-catalogo.md. Toca ticketmaster.py, que é da Story 2.1,
  # e uma linha de publicar/page.tsx, que é da 2.2. Não vira story de propósito —
  # o motivo está no README da raiz.
  # Code review da epic inteira em 2026-08-11 (bmad-code-review, 3 camadas):
  # 4 decisões do Igor, 16 patches aplicados, 7 adiados, 11 descartados. Achados
  # e triagem em code-review-epic-2.md; adiados em deferred-work.md. A suíte foi
  # de 203 para 218 testes. Fica 'in-progress' até o merge na main, como a Epic 1.
  epic-2: done
  2-1-cliente-da-ticketmaster-com-a-chave-protegida: done
  2-2-buscar-a-atracao-no-catalogo: done
  2-3-modelo-de-evento-e-setor: done
  # A 2.4 contraria o AD-7 por uma story de distância: ela publica sem exigir
  # portaria escalada, e a 2.5 é quem acrescenta o `EVENTO_SEM_PORTARIA` à mesma
  # rota. Decisão consciente: a janela dura uma story, numa branch que só o Igor
  # publica, e fechá-la agora custaria retrabalho na 2.5. Registrada no AC18 da
  # story e no README da raiz.
  2-4-publicar-um-evento-com-seus-setores: done
  # A 2.5 é quem paga a dívida do AD-7 aberta na 2.4: `EVENTO_SEM_PORTARIA` entra
  # na mesma rota `POST /organizador/eventos`, e a linha da janela em
  # `README.md#o-que-não-está-pronto` é reescrita (AC20). Decisões do Igor:
  # lista por rota própria com busca pelo nome, vários escalados por evento, e
  # uma segunda conta de portaria no seed.
  2-5-escalar-quem-valida-na-porta: done
  # A última da epic, e a primeira story só de leitura. Decisões do Igor: sem
  # editar nada (o "gerenciar" do título é acompanhar), lista enxuta + página de
  # detalhe, corte em "Em cartaz" e "Já aconteceram", e `Meus eventos` entrando
  # no masthead com a confirmação de publicação apontando para lá — sem redirect.
  2-6-ver-e-gerenciar-meus-eventos: done
  epic-2-retrospective: optional

  # ---------- Epic 3: Descoberta e compra ----------
  # Code review da epic inteira em 2026-08-12, e o primeiro em **três camadas ×
  # três grupos**: nove subagentes, com o diff recortado por fronteira de
  # transação (leitura pública, reserva→pagamento→ingresso, frontend) em vez de
  # por tamanho. 7 decisões do Igor, 31 patches aplicados, 18 adiados, 9
  # descartados. A suíte foi de 379 para 395 testes. Achados e triagem em
  # code-review-epic-3.md; adiados em deferred-work.md.
  #
  # Os três achados de alta eram todos invisíveis para a suíte de antes: a
  # resposta de `POST /reservas/{id}/pagamento` devolvia `PENDENTE` e
  # `ingressos: []` em produção, por divergência de `expire_on_commit` entre o
  # `SessaoLocal` e o `conftest`; o `ESTOQUE_INSUFICIENTE` **parcial** (restam 3,
  # pediu 4) travava a tela num laço com a frase errada, porque o handler supunha
  # que o código significa "esgotou"; e o checkout pedia cartão real ao navegador
  # pelos `autoComplete="cc-*"`.
  #
  # Dois defeitos de teste apareceram durante o conserto, e os dois são do tipo
  # que o review existe para achar: a fixture `cliente` precisou de
  # `expire_all()` por requisição para imitar produção, e adulterar o **último**
  # caractere de uma assinatura base64 não adultera nada em ~4,7% dos casos — o
  # que fazia o teste de forja do ingresso passar sem ter forjado.
  #
  # Fechada em 'done' com o merge da branch na main.
  epic-3: done
  # A primeira rota pública do projeto (`GET /eventos`, no primeiro
  # `app/api/publico.py`) e a raiz deixando de ser o estado vazio da 1.2.
  # Decisões do Igor: eventos passados somem, com o corte no backend; o "a
  # partir de" conta só setor com ingresso; a fila já linka para `/eventos/{id}`,
  # abrindo uma janela de três stories até a 3.4 fechá-la; e `imagem_url` fica
  # fora do contrato até a 3.3. O AC7 é o que mais importa: `capacidade`,
  # `vendidos` e `setores` não atravessam o `response_model` — UX-DR7 se garante
  # no contrato, não na tela.
  3-1-ver-a-programacao: done
  # Três decisões do Igor: a peneira roda no `where` do Postgres com a busca na
  # URL (nada de filtrar em JavaScript — a raiz continua Server Component);
  # período e cidade vindos do banco, com `GET /eventos/cidades` alimentando os
  # chips; e a busca ignorando acento pela extensão `unaccent`, o que traz a
  # primeira migração deste projeto que não cria tabela. ✅ RESOLVIDO em
  # 2026-08-11: o risco de o Postgres da Railway recusar `CREATE EXTENSION` foi
  # conferido no painel — usuário `postgres` com `usesuper = true`, extensão
  # 1.1 disponível e já criada à mão, então a migração é no-op lá. O registro
  # está no docstring da migração `06c1ad5ac276`. Nada pendente.
  3-2-buscar-e-filtrar-a-programacao: done
  # Seis decisões do Igor: a arte chega por **rota própria** (`GET /eventos/destaque`,
  # com `EventoEmDestaque`) em vez de `imagem_url` entrar no `EventoNaProgramacao`
  # — a fila continua sem carregar campo que não lê; **o standfirst não existe**,
  # porque o `Evento` não tem texto livre e frase montada com os dados do kicker é o
  # anti-padrão nº 5 do DESIGN.md (registrado só na story, sem ir a README, por
  # instrução dele); a capa **some com filtro ativo**, e nem é buscada; o destaque
  # **sai da fila**, cortado por `id`; a ficha é `CASA · CIDADE · SETORES`, com os
  # nomes dos setores; e o destaque **esgotado continua na capa**, com selo e sem
  # link. ⚠️ A armadilha da story é o teste de varredura de palavras proibidas da
  # 3.1: `setores` e `imagem_url` viram chaves legítimas nesta resposta.
  3-3-chamada-principal-na-programacao: done
  # Sete decisões do Igor: **o endereço não existe** (o `Evento` não tem a coluna,
  # e a ficha mostra `CASA` e `CIDADE`); **o teto do stepper é fixo por compra**
  # (`maximo_por_compra: 6` no contrato, e não `min(disponivel, 6)`) — é o que
  # permite o stepper ter teto sem o contrato revelar estoque, com a recusa do
  # excesso ficando para o `UPDATE` condicional do AD-3 na 3.6; **o rodapé não tem
  # botão**, porque reservar é a 3.6; **"Últimos ingressos" é 20% ou menos**;
  # **a arte fica ao lado do cabeçalho**; **o recorte é o mesmo das outras três
  # rotas públicas**, com rascunho, passado e inexistente no mesmo `404`; e
  # **vários setores somam** na mesma escolha. ⚠️ Duas armadilhas: a lista de
  # palavras proibidas do teste é a **terceira** diferente do arquivo (agora
  # `preco_centavos` é chave legítima), e `/eventos/{id}` precisa ser declarada
  # **depois** de `/eventos/cidades` e `/eventos/destaque` — é esta a story que
  # exercita o comentário de ordem escrito na 3.2.
  3-4-ver-o-evento-e-seus-setores: done
  # A story gêmea da 2.3, e a primeira migração desde a 3.2 que cria tabela:
  # duas tabelas, nenhum consumidor, o critério de pronto é o schema. Quatro
  # decisões do Igor: `item_reserva` com `id` UUID próprio + `UNIQUE (reserva_id,
  # setor_id)`, e não a PK composta da `evento_portaria`; apagar evento, setor ou
  # conta com reserva é **recusado pelo banco** (as três FKs sem `ondelete`), o
  # que faz o `CASCADE` de `setor` deixar de valer na primeira venda; só
  # `criado_em` além do `expira_em`; e `estado` como `String(20)` + `CHECK`, no
  # precedente do `usuario.papel`, nunca `ENUM` nativo. ⚠️ Os ACs 3 e 4 repetem
  # de propósito o AC9 da 2.3: as duas transições condicionais do AD-4 provadas
  # por `.rowcount` antes de existir service. `frontend/` e o README da raiz não
  # são tocados.
  3-5-modelo-de-reserva: done
  # A garantia mais pontuada do desafio, e a primeira escrita do lado do cliente:
  # `setor.vendidos` sai de zero pela primeira vez. Cinco decisões do Igor: **a
  # tela vai até `/reservas/{id}`**, com botão na página do evento e o cronômetro
  # ali, ficando o pagamento para a 3.8; **o visitante vai ao login e volta** pelo
  # `?voltar=` que já existe desde a 1.4, e **a escolha do stepper se perde** de
  # propósito (o estoque pode ter mudado nesses segundos); **o `409` se resolve
  # relendo `GET /eventos/{id}`** e montando a frase do UX-DR8 com dados frescos —
  # o corpo do erro continua `{codigo, mensagem}`, sem exceção ao formato único do
  # `core/erros.py`; e **os 10 minutos são constante do service**, no precedente do
  # `MAXIMO_POR_COMPRA`, não variável de ambiente. ⚠️ Duas armadilhas: ler
  # `setor.vendidos` para dentro do Python "só para conferir antes" é o AD-3
  # quebrado com o resultado certo em 99% dos casos; e a corrida **não** se prova
  # pelo `TestClient` — a fixture `cliente` amarra o app a uma sessão só, então o
  # teste precisa de duas `Session` em conexões distintas, com commit e limpeza.
  3-6-reservar-sem-vender-o-mesmo-lugar-duas-vezes: done
  # ⚠️ MUDANÇA DE FORMATO, 2026-08-12. Daqui em diante não existe mais arquivo de
  # story: as três são especificadas juntas numa techspec em docs/, e o README
  # deixa de ser escrito a cada commit (passa inteiro para a Epic 6). O motivo e
  # as regras estão em CLAUDE.md, seção "Techspec no lugar de story".
  # As três viram uma spec só porque os próprios ACs as costuram na mesma função
  # `pagar_reserva`: a expiração da 3.7 dispara dentro da rota que a 3.8 cria, e a
  # emissão da 3.9 acontece na mesma transação da transição para PAGA (AD-14).
  # O commit continua um por story — a spec agrupa, o histórico não.
  3-7-reserva-abandonada-devolve-o-estoque: done
  # Commit 2: `PaymentGateway` (Protocol) + `PagamentoSimulado`, a rota
  # `POST /reservas/{id}/pagamento` e o checkout. Fecha o AC1 da 3.7, que o
  # commit da 3.7 deixou aberto de propósito. Decisões do Igor: `402` para a
  # recusa (não é conflito de estado, é resposta do gateway); checkout com
  # nome/e-mail/CPF/telefone mais escolha entre cartão e Pix, com o Pix gerando
  # QR e código falsos na tela e um botão "cobrança paga" como atalho do
  # avaliador; nada persistido além do nome; e CPF só de formato — o dígito
  # verificador recusaria o dado fictício que a própria tela manda usar.
  # ⚠️ A armadilha da story: os dois caminhos de recusa dão `commit` **antes**
  # do `raise`, o oposto do `criar()` da 3.6. A reserva precisa ficar
  # `RECUSADA`/`EXPIRADA` com o estoque devolvido; sem o commit, o status sai
  # idêntico e o estoque nunca volta — falha invisível fora do banco.
  3-8-pagar-com-aprovacao-e-com-recusa: done
  # Commit 3, e o fim da techspec: tabela `ingresso` (sétima do schema), emissão
  # de um ingresso por unidade dentro da transação que marca `PAGA` (AD-14), e o
  # código `ID.ASSINATURA` com HMAC-SHA256 (AD-5). Decisões do Igor:
  # `TICKET_SIGNING_SECRET` como variável própria — girar a de sessão passaria a
  # invalidar ingresso emitido; a coluna `assinatura` guardada só para montar o
  # QR, com a validação sempre recalculando; e `titular_nome` vindo do campo do
  # checkout, não de `usuario.nome`. ⚠️ Duas armadilhas: a emissão tem que ficar
  # **dentro** do ramo que vence o `_transicionar`, senão reprocessar emite em
  # dobro sem quebrar nenhum teste sequencial; e `hmac.compare_digest`, nunca
  # `==`. O QR desenhado é da Story 4.2 — aqui nasce o código, não a imagem.
  3-9-receber-ingressos-com-codigo-nao-forjavel: done
  epic-3-retrospective: optional

  # ---------- Epic 4: Meus ingressos e compartilhamento ----------
  # MUDANÇA DE FORMATO, seguindo a 3.7-3.9: a 4.1 e a 4.2 saem de uma
  # techspec só (docs/techspec-meus-ingressos.md) — o grupo de leitura do
  # mesmo agregado, lista e detalhe pelo mesmo caminho (reserva → cliente da
  # sessão), com o mesmo 404 de "não é seu". O commit continua um por story.
  #
  # Code review da epic em 2026-08-12, em **escopo estreito por pedido do Igor**:
  # só o que quebra código ou deploy, reportando apenas Critical e High. Três
  # camadas em paralelo (backend, frontend, testes), recortadas por fronteira de
  # autenticação — quem exige sessão contra quem não exige, que é a invariante
  # nova desta epic. Saldo: **zero Critical, 4 High, todos os 4 corrigidos**. A
  # suíte foi de 449 para 451. Achados em code-review-epic-4.md.
  #
  # ⚠️ **A lição desta epic: os quatro achados foram provados por mutação, e um
  # deles desmentiu a própria camada que o levantou.** A camada afirmou que um
  # `share_token` sequencial passaria nos 449 testes; com contador ela erra (o
  # "1" cai dentro do UUID do código e um teste existente pega por acaso), mas
  # com `token_urlsafe(4)` — 32 bits, força-bruta viável — os 53 testes de
  # compartilhamento e migração passam **todos**. O achado era real e a
  # justificativa dele não era. Achado de revisor vale o que a mutação provar.
  #
  # Os três eixos que build verde e suíte verde não cobriam: corrida de escrita
  # (o `compartilhar` era read-then-write sem trava), teste que passa nos dois
  # sentidos (o "sem recalcular" comparava contra o valor que o recálculo também
  # produz), e garantia declarada sem asserção nenhuma (a entropia do token).
  epic-4: done
  # Commit 1: migração de `usado_em`/`validado_por` (AD-6, adiantada da 5.2
  # porque o primeiro consumidor é a leitura, não a porta), `services/
  # ingresso.py` (arquivo novo — o agregado tem vida própria desde a 3.9),
  # `GET /ingressos` e a tela `/ingressos` com os blocos Ativos/Utilizados.
  # ⚠️ O bloco Utilizados nasce vazio de propósito: nada escreve `usado_em`
  # antes da Story 5.2 existir — provado por teste que grava a coluna à mão.
  4-1-ver-meus-ingressos: done
  # Commit 2: `GET /ingressos/{id}` (IngressoDetalhe, com `codigo` montado da
  # coluna sem recalcular — AD-5), a tela do canhoto com QR em duas colunas
  # (talão/corpo com picote tracejado), e `/reservas/{id}` enxugada: perde os
  # canhotos, ganha a confirmação mais o link para `/ingressos`. Decisão da
  # techspec: o canhoto cheio existe só em `/ingressos/{id}` — duas versões
  # do mesmo canhoto convivendo obrigaria quem lê a decidir qual é o de
  # verdade. `bgColor`/`fgColor` do QR **em hex literal** (`#e4ebea`/`#0b1618`),
  # como a spec pedia: eles viram atributo `fill` do SVG, e `var()` que não
  # resolva ali cai para o preto padrão — QR preto sobre preto, invisível para
  # a suíte e detectável só na fila da porta. Chegaram como `var(--cal)`/
  # `var(--breu)` e voltaram ao hex no code review da Epic 4.
  4-2-ver-o-canhoto-com-o-qr: done
  # A 4.3 e a 4.4 saem de outra techspec só (docs/techspec-link-compartilhado.md):
  # um recurso e seu ciclo de vida — o `share_token` nasce na 4.3 e morre na
  # 4.4, na mesma coluna, no mesmo subrecurso e no mesmo service.
  # Commit 1: migração `ed0bb0dad2a3` (`share_token`, anulável, índice único),
  # `gerar_share_token()`, `POST /ingressos/{id}/compartilhamento` (idempotente:
  # com link ativo devolve o mesmo token, porque um token novo a cada clique
  # seria revogação silenciosa), a rota pública `GET /ingressos/compartilhados/
  # {token}` em `publico.py`, o `<Canhoto>` extraído e a tela `/i/{token}`.
  # ⚠️ Janela consciente de um commit: aqui existe link gerado sem como
  # revogar — a mesma forma da janela entre a 2.4 e a 2.5, e pelo mesmo motivo.
  # Fecha no commit da 4.4.
  # ⚠️ `/ingressos` passou a morar em dois routers, e `cliente.py` é registrado
  # antes: o que salva a rota pública são os três segmentos do caminho contra os
  # dois da autenticada. Há teste provando as duas de pé.
  4-3-compartilhar-o-ingresso-por-link: done
  # Commit 2: `DELETE /ingressos/{id}/compartilhamento` → 204 (o primeiro
  # DELETE da API), `Botao` com a prop `variante` que o docstring dele previa
  # desde a 1.2, `<Confirmacao>` com <dialog> nativo e o gatilho *Revogar link*
  # na ilha. A janela do commit anterior fechou aqui.
  # ⚠️ `showModal()`, nunca `show()`: o segundo não trava foco, não põe
  # backdrop e não torna a página inerte — parece funcionar e não é modal.
  # ⚠️ 204 também quando já não havia link: o DELETE é idempotente, e revogar
  # duas vezes não é erro. Token revogado responde igual a token que nunca
  # existiu — é isso que faz a revogação ser um corte, não um aviso.
  # ⚠️ O `close()` do <dialog> ENFILEIRA o evento `close`; a ilha desliga o
  # estado de confirmação na mão antes de chamar a API, senão a desmontagem
  # do diálogo no sucesso engole o evento e a caixa reabre sozinha depois.
  4-4-revogar-o-link-compartilhado: done
  epic-4-retrospective: optional

  # ---------- Epic 5: Portaria ----------
  # Techspec em docs/techspec-turnos-da-portaria.md. Cobre **uma** story só, e
  # mesmo assim é techspec: a 5.1 cria a terceira casca do frontend, muda o
  # fluxo de acesso da Story 1.4 e decide a régua de tempo que a epic inteira
  # herda. As 5.2 a 5.6 saem de outras specs.
  epic-5: in-progress
  # Commit 1: `GET /portaria/eventos` (primeira leitura da `evento_portaria`,
  # gravada desde a 2.5 e sem consumidor até aqui), a casca própria em
  # `/portaria` — sem grupo de rotas, sem o masthead do `(site)` —, a lista de
  # turnos, `/portaria/conta` com o `<DadosDaConta>` extraído, e o login
  # mandando a portaria para a casa dela. **Sem migração.**
  # Decisões do Igor: casca própria em vez de item no masthead (EXPERIENCE.md
  # manda a portaria ter navegação própria); o nome de quem está na porta
  # aparece na casca, contrariando o Masthead da 1.6, porque o celular da porta
  # é compartilhado; o `?voltar=` continua soberano e a regra por papel só vale
  # quando ele está **ausente**; e a lista **não corta por tempo** — a portaria
  # trabalha do outro lado do corte que as rotas públicas fazem.
  # ⚠️ **A conferência visual achou o que a suíte não tinha como achar: a casca
  # nova tinha três saídas e nenhuma entrada.** Três correções, todas do mesmo
  # defeito — casca que não leva de volta para si mesma é um beco:
  #   1. O `Logotipo` ganhou `href`/`rotulo`. O padrão fixo em `/` mandava quem
  #      está na porta para a programação pública. Padrão inalterado, então
  #      `(site)` e `(entrada)` não mudaram uma linha.
  #   2. A `/conta` do `(site)` rebate `PORTARIA` para `/portaria/conta`. É a
  #      única entrada da casca a partir do site, e entra **sem** tocar no
  #      masthead, por um link que ele já oferece. A alternativa descartada era
  #      o item `TURNOS` no masthead, que custaria exatamente a decisão que a
  #      casca própria comprou. De quebra acaba com `/conta` e `/portaria/conta`
  #      mostrando a mesma tela em duas cascas conforme o endereço digitado.
  #   3. O `not-found.tsx` da **raiz** passou a escolher a casca pelo papel. Ele
  #      é o único que atende URL sem rota, roda fora de qualquer layout de
  #      grupo e por isso carrega a casca à mão — carregava *uma*, a de jornal.
  #      Clicar num turno enquanto `/portaria/eventos/{id}` não existe devolvia
  #      `Início`/`Minha conta` e nenhum caminho para `/portaria`; a leitura era
  #      "eu nem estou mais logado". Para isso o cabeçalho saiu do layout e
  #      virou `components/CabecalhoDaPortaria.tsx`, usado pelos dois.
  # Mais uma decisão do Igor, de aparência: **a casca usa `.conteudo`**, a mesma
  # medida do resto do produto. A primeira versão fechava em 560px pelo
  # "primeiro para telas pequenas" do EXPERIENCE.md, e na tela a diferença de
  # largura leu como defeito de layout. O que sobrevive do mobile-first é o que
  # importa na fila: alvo de 44px, tipografia grande, uma coluna só.
  # ⚠️ Janela consciente de **dois commits**: o item aberto já linka para
  # `/portaria/eventos/{id}`, que só nasce na 5.3. Mesma forma da janela entre a
  # 2.4 e a 2.5 e da fila da 3.1. Fecha no commit da 5.3.
  # ⚠️ O portão abre `data_hora - 2h`, e não em `data_hora` como a resposta
  # inicial dizia: `publicar` recusa data no passado e as rotas públicas
  # escondem o evento a partir de `data_hora`, então portão exato travaria o
  # roteiro de avaliação da 6.4. É constante de uma linha na tela.
  5-1-ver-onde-eu-trabalho-hoje: done
  # Fora da numeração, entre a 5.1 e a 5.2: **o código do ingresso encolheu de 80
  # para 8 caracteres**. Commit `feat` avulso, spec em
  # docs/techspec-codigo-curto.md. Toca `core/seguranca.py`,
  # `models/ingresso.py`, a emissão em `services/reserva.py`,
  # `services/ingresso.py` e uma migração — todos de outras epics. Não vira story
  # porque nenhuma das 38 previu isto: é consequência de uma decisão de produto
  # tomada ao escrever a spec da validação na porta, e o campo manual da 5.3 é
  # inutilizável com 80 caracteres na fila.
  # Decisões do Igor: 8 caracteres (40 bits) em base32 de Crockford, com o HMAC
  # **truncado** e não sorteado — é o que mantém a frase do AD-5 de pé; e o
  # ingresso passa a estar no nome da **conta**, com `titular_nome` virando
  # `pagador_nome` na coluna. Nenhum ingresso emitido foi invalidado: a migração
  # calcula o código de cada linha em vez de zerar a tabela.
  # ⚠️ A `docs/techspec-validacao-na-porta.md` **assume este commit aplicado**.
  # ⚠️ Colisão de código deixou de ser impossível com 40 bits: a emissão sorteia
  # outro `nonce` dentro de um `begin_nested()`. Sem o SAVEPOINT o
  # `IntegrityError` mata a transação que acabou de marcar a reserva `PAGA`, e o
  # pagamento aprovado termina em `500`.
  # ⚠️ O `U` não está no alfabeto de Crockford (nem `I`, `L` e `O`). Quem testar
  # à mão não invente código com `U` e conclua que a validação quebrou.
  # ⚠️ A migração lê o `TICKET_SIGNING_SECRET` para calcular os códigos: girar o
  # segredo **depois** dela invalida o que ela calculou, como sempre invalidou.
  # Techspec em docs/techspec-validacao-na-porta.md, que cobre 5.2 + 5.3 + 5.5:
  # um endpoint de validação e as duas formas de entrada que o alimentam. O
  # commit continua um por story — este é o commit 1 dos três.
  #
  # Commit 1: `POST /portaria/eventos/{id}/validacoes`, a dependência
  # `exigir_porta_aberta` (que junta o vínculo do AD-7 e o portão numa recusa
  # só), `ingresso.validar` com o `UPDATE` condicional do AD-6, e o campo
  # `aberto` em `TurnoDaPortaria`. **Sem migração** — `usado_em` e `validado_por`
  # vieram na 4.1, e o código curto veio na spec anterior.
  # Decisões do Igor: os quatro vereditos respondem `200` (recusar entrada é o
  # trabalho da portaria dando certo, e o `ErroDeDominio` não teria onde pôr a
  # hora da primeira entrada); `EVENTO_ERRADO` **não** diz de qual show o
  # ingresso é, contra o protótipo do EXPERIENCE.md, porque devolver o nome do
  # evento a quem não foi escalado nele é o que o AD-7 existe para impedir;
  # código bem formado de ingresso nenhum é colapsado com assinatura divergente,
  # sem quinto veredito; e o evento vai no **caminho**, não no corpo, que é o que
  # permite o `403` sair de uma dependência em vez de um `if` no handler (AD-9).
  # ⚠️ **Esta story reverte duas decisões da 5.1, e as duas reversões são o
  # ponto.** A spec da 5.1 dizia que o portão de 2h era conveniência da tela e
  # que o contrato não carregaria campo derivado. Estava errado por um motivo
  # concreto: uma chamada três dias antes do show grava `usado_em`, e na noite do
  # evento a pessoa é barrada com `JA_UTILIZADO` sem nunca ter entrado — a tela
  # impedia, a rota não, e credencial de portaria mais um `curl` bastavam. A
  # regra desceu para `ABERTURA_DOS_PORTOES` em `services/evento.py`, virou
  # `403 EVENTO_NAO_ABERTO` e `TurnoDaPortaria.aberto`, e a tela apagou a
  # constante. Uma regra, um relógio, um lugar: com duas constantes de duas horas
  # em duas camadas, um dia a tela mostraria a porta aberta e a API recusaria.
  # ⚠️ `validar` é a primeira função de `services/ingresso.py` que **não** passa
  # pelo `_carregar_do_cliente`, cujo docstring diz "toda rota do dono passa por
  # aqui". Aqui não há dono: quem chama é um terceiro autorizado e o `where` é o
  # código, não o `cliente_id`. O aviso está escrito nos dois docstrings.
  # ⚠️ A assinatura se confere contra o `evento_id` **do ingresso**, nunca contra
  # o do caminho — contra o contexto, um código legítimo de outro show sairia
  # `INVALIDO` em vez de `EVENTO_ERRADO`, dois vereditos trocados. E o
  # `EVENTO_ERRADO` vem **antes** do `UPDATE`: invertido, o ingresso do outro
  # show seria queimado por uma portaria que nem podia lê-lo.
  # ⚠️ `expire_on_commit=False` pela terceira vez (depois do pagamento na Epic 3
  # e do `share_token` na 4.3): o `RETURNING` resolve o `VALIDO`, e o
  # `JA_UTILIZADO` **relê a linha** — sem isso a resposta diz "já utilizado" sem
  # dizer quando, que é a única informação que resolve a discussão na porta.
  # ✅ **Provado por mutação, não por leitura:** trocar o `UPDATE` condicional
  # pelo `if ingresso.usado_em is None` em Python passa nos **19** testes
  # sequenciais do arquivo e falha só no de duas conexões — que é exatamente a
  # classe de bug que o desafio avalia, e a lição registrada na Epic 4.
  # ⚠️ Janela consciente de um commit: a tela `/portaria/eventos/{id}` ainda não
  # existe (nasce na 5.3), então o link do turno aberto continua levando a
  # lugar nenhum. É a mesma janela que a 5.1 abriu, e ela fecha no commit da 5.3.
  5-2-validar-o-ingresso-sem-deixar-passar-duas-vezes: done
  # Commit 2 da mesma techspec: `GET /portaria/eventos/{id}`, a tela
  # `/portaria/eventos/{id}` e a ilha `<Leitor>` com o campo manual, mais
  # `lib/validacao.ts`. **Sem migração.**
  # A rota nova existe porque `GET /eventos/{id}` **não serve**: ela corta em
  # `data_hora >= agora` e responde `404` exatamente durante o show, que é quando
  # a portaria trabalha. Descartado buscar a lista inteira e achar o item pelo id
  # na tela — funciona, e faz a tela de um turno depender de uma rota que fala de
  # todos os outros. O handler é o mais curto da API: a dependência do commit
  # anterior já traz as duas recusas, sem uma linha nova.
  # ⚠️ **`lib/validacao.ts` é separado do `lib/turnos.ts` por razão física**, não
  # de estilo: turnos importa `servidor.ts` → `next/headers`, e `next/headers`
  # alcançado por um componente `"use client"` quebra o build. O leitor é ilha.
  # ⚠️ **Todo `403` da tela vira redirect para `/portaria`**, e os códigos não são
  # separados: `SEM_ESCALA_NO_EVENTO` e `EVENTO_NAO_ABERTO` são o mesmo fato para
  # quem lê — este turno não é seu, agora. Um `notFound()` diria que o show não
  # existe, o que é falso, e mandaria procurar o defeito no lugar errado.
  # `indisponivel` é o único que **não** redireciona: vaivém entre duas telas que
  # não carregam nada é pior que uma frase.
  # ⚠️ **A tela não reconfere `turno.aberto`.** Se a API respondeu `200`, a
  # dependência já deixou passar; conferir de novo aqui seria ressuscitar a
  # segunda constante de duas horas que a 5.2 acabou de eliminar.
  # Decisões desta tela: `<form onSubmit>` em vez de `onKeyDown`, porque a
  # submissão implícita de formulário de um campo só é nativa e já funciona com o
  # "Ir" do teclado do celular; botão *Validar* **além** do Enter, de largura
  # inteira (EXPERIENCE.md#Responsive), porque nem todo teclado de celular oferece
  # a tecla de confirmar; e o `<h1>` é o **nome do show**, não a palavra "Leitor"
  # — é ele que responde à única pergunta antes de começar, "é este o show
  # certo?", e quem trabalha em duas casas na mesma noite abre o leitor errado
  # com facilidade.
  # ⚠️ **O foco volta ao campo no `finally`, nos dois caminhos.** Sem isso, depois
  # de clicar em *Validar* o foco fica no botão e o Enter seguinte reenvia o mesmo
  # código — defeito que só aparece para quem usa o botão, que é justamente quem
  # está com uma mão só.
  # ⚠️ **O veredito sai da tela na submissão seguinte, não por tempo**
  # (EXPERIENCE.md#veredito). E ele é limpo no **início** da chamada, não quando a
  # resposta chega: durante a ida à rede ele estaria respondendo pela leitura
  # errada, e a fila lê o que está na tela.
  # **Fora de propósito, e vai junto na 5.4:** cor, símbolo e corpo de 46px. Meio
  # caminho seria pior que nenhum — veredito colorido sem símbolo transmite por
  # cor sozinha, que é o que a regra dos três canais proíbe. O `aria-live=
  # "assertive"` **entrou agora**, contra o que a spec previa: é piso de
  # acessibilidade do EXPERIENCE.md, e uma tela que anuncia nada para leitor de
  # tela não é "simples", é incompleta.
  # ✅ **A janela aberta pela 5.1 fechou:** o link do turno chega numa tela que
  # existe. Eram dois commits, a mesma forma da janela entre a 2.4 e a 2.5.
  5-3-digitar-o-codigo-quando-a-camera-nao-ajuda: done
  # Commit 1 da techspec docs/techspec-veredito-e-contador.md. `Veredito.tsx` +
  # `.module.css` — a função `Veredicto` saiu de dentro do `Leitor.tsx` (a grafia
  # certa foi junto), mais os quatro símbolos e a tinta. **Só frontend**: nenhuma
  # linha de backend, nenhuma migração.
  # Decisões do Igor: **duas tintas, e não quatro** — `--verde` no `VALIDO`,
  # `--brasa` nos outros três. Contraria um AC escrito da 5.4 ("`JA_UTILIZADO`
  # usa tom neutro") e a tabela do `EXPERIENCE.md`, de propósito: na porta a
  # pergunta é binária, e quatro tinturas obrigam quem está com a fila esperando
  # a decodificar qual apareceu antes de saber o que fazer. Descartadas as quatro
  # cores, que exigiriam um `--ambar` e um cinza novos num sistema de duas tintas.
  # ⚠️ **UX-DR5 continua cumprida, e é preciso ver por quê:** a regra proíbe
  # transmitir informação **só** por cor. Cor, palavra e símbolo continuam nos
  # quatro; o que encolheu foi a cardinalidade de um canal, não a quantidade.
  # ⚠️ **Os símbolos são SVG inline, não caractere de fonte.** O `⤫` é
  # matemático e vira retângulo vazio no Android ou num Windows sem fonte de
  # símbolos — justamente no canal que existe para não depender de cor.
  # ⚠️ **`EVENTO_ERRADO` virou X dentro de círculo**, e a spec pedia `⤫`. Quase
  # toda fonte desenha o `⤫` igual a um `✕`, e copiá-lo apagaria a distinção
  # entre `INVALIDO` e `EVENTO_ERRADO` bem no canal que a decisão das duas tintas
  # tornou o mais importante. A três metros o que separa é a silhueta.
  # ⚠️ **A tinta entra por `data-veredito` e o switch é no CSS** — nunca um
  # `estilos[resultado]` em JavaScript, que o `tsc` não confere e que quebra em
  # silêncio quando um nome de classe some do CSS Module.
  # ⚠️ **O bloco subiu para cima do formulário**, e o `.painel` trocou
  # `border-top` por `border-bottom`: a três metros o que se lê é o topo da tela,
  # e o campo com a dica empurrava o resultado para a dobra no celular. Sem botão
  # *Ler o próximo* (o `EXPERIENCE.md` pedia): a câmera já se desliga sozinha, e
  # o toque seguinte produz o mesmo estado.
  # ⚠️ **`clamp(38px, 11vw, 46px)` na palavra, mais `overflow-wrap`.** Em 360px
  # `UTILIZADO` a 46px mede 273px contra 276px disponíveis — três pixels de
  # folga, perto demais para deixar fixo. Abaixo de ~310px o piso do `clamp` não
  # salva, e é o `overflow-wrap` que impede a palavra de atravessar a borda.
  # 📌 **Adiado, decisão do Igor:** `VALIDO` não mostra a hora da entrada, embora
  # o `EXPERIENCE.md` peça e o `entrada_em` chegue no contrato. É herança da 5.3
  # e a spec congelou o texto do `detalhe()`; fica registrado porque a hora é o
  # dado que encerra discussão na porta e some no caso majoritário.
  5-4-ver-o-resultado-a-tres-metros: done
  # Commit 3 e último da techspec da validação na porta. `@zxing/browser` no
  # `package.json`, o botão *Ler pela câmera* na ilha `<Leitor>` e os estados de
  # permissão. **Sem migração e sem uma linha de backend** — a rota da 5.2 já
  # atende: a câmera é só uma segunda forma de preencher o mesmo campo.
  # Decisões do Igor: câmera **opt-in por botão**, nunca pedida ao abrir a página
  # — negada uma vez, o navegador lembra, e recuperar exige mexer nas
  # configurações do site.
  # ⚠️ **`import()` dinâmico em vez de `next/dynamic`, e a spec pedia
  # `next/dynamic`.** Aquele embrulha **componente**, e `@zxing/browser` é
  # biblioteca; o mecanismo que entrega o mesmo efeito é o `import()` dentro do
  # efeito, que já é só do navegador por construção. O corte foi conferido no
  # build, não suposto: os dois chunks com o zxing (**473 KB**, bem acima dos
  # ~200 kB que a spec estimava) **não** aparecem no `build-manifest.json` da
  # rota. Descartada a `BarcodeDetector` nativa — zero bytes, e inexistente no
  # Safari do iPhone, que é metade da fila.
  # ⚠️ **`decodeFromConstraints` com `facingMode: environment`, e não
  # `decodeFromVideoDevice(undefined)`** como a spec escreveu. O segundo pega o
  # primeiro dispositivo da lista, que no celular costuma ser a câmera
  # **frontal**: a portaria apontaria o telefone para o próprio rosto. `ideal` e
  # não `exact`, senão um notebook de webcam única falharia como "câmera ausente"
  # e a tela mentiria.
  # ⚠️ **A trava do "mesmo QR duas vezes" é `useRef`, não `useState`.** O zxing
  # chama o callback a cada quadro decodificado, e um `setState` só chegaria no
  # render seguinte — depois de duas ou três leituras já terem passado. Sem timer
  # nenhum: o scanner para na primeira leitura e só reabre no próximo pedido.
  # ⚠️ **Existe corrida entre o primeiro quadro e a resolução da promessa do
  # `decodeFromConstraints`**: um QR já enquadrado quando a câmera abre é
  # decodificado antes de os controles existirem, e aí o `stop()` do callback não
  # tem o que parar. Tratada no ramo que confere a trava depois do `await`.
  # ⚠️ **O `stop()` na desmontagem tem efeito próprio com `[]`.** Sem ele o
  # `MediaStream` fica preso e a luz da câmera continua acesa depois de navegar,
  # o que na porta lê como aplicativo travado.
  # ⚠️ **O `autoFocus` do campo saiu**, e ele foi posto na 5.3: com a câmera como
  # caminho primário, o teclado subindo sozinho cobria metade da tela — botão e
  # mira inclusive. O foco continua sendo devolvido ao campo depois de cada
  # validação **digitada**, e não depois das lidas pela câmera, pelo mesmo motivo.
  # ⚠️ A câmera vem **antes** do campo na tela, seguindo a jornada 2 do
  # EXPERIENCE.md: Ana aponta a câmera e só digita quando a leitura falha.
  # ✅ `README.md#o-que-não-está-pronto` ganhou a linha do `getUserMedia` em
  # contexto seguro, escrita neste commit — a exceção do CLAUDE.md.
  5-5-ler-o-qr-pela-camera: done
  # Commit 2 e último da techspec do veredito e do contador — **fecha a Epic 5**.
  # Tabela `validacao` com migração (e5bf44a9f826), o `INSERT` nos quatro
  # caminhos do `validar`, `contar_entradas_por_evento` e `contar_recusas` em
  # `services/ingresso.py`, os campos novos nos três schemas e a ilha
  # `ContadorDoTurno`. A suíte foi de 498 para 506.
  # Decisões do Igor: **persistir tentativa, e não só entrada** — contar os
  # quatro vereditos exige guardar os quatro, e até aqui `INVALIDO`,
  # `JA_UTILIZADO` e `EVENTO_ERRADO` eram resposta HTTP que acabava ali.
  # Descartado contar só as entradas (o AC original) e descartado contar no
  # `useState` da tela, que zeraria ao recarregar — exatamente a coisa validada
  # só no frontend que este projeto é avaliado por não fazer. O que se ganha além
  # do número é a trilha de auditoria: quem estava na porta, o que leu e o que o
  # sistema respondeu.
  # ⚠️ **Duas fontes para números vizinhos, de propósito.** `entradas` sai de
  # `ingresso.usado_em`, que o `UPDATE` condicional do AD-6 escreve
  # atomicamente; as três recusas saem da `validacao`. No dia em que divergirem,
  # o `usado_em` ganha — e um teste confere que não divergiram.
  # ⚠️ **O `INSERT` é da mesma transação do `UPDATE`.** Fora dela existiria a
  # janela em que alguém entrou e o registro não saiu. Provado por teste que
  # força a FK a estourar depois do `UPDATE` e confere o `usado_em` de volta a
  # nulo.
  # ⚠️ **As contagens são lidas depois do `commit`, nunca antes** — quarta
  # aparição desta família de armadilha no projeto (pagamento na 3.8,
  # `share_token` na 4.3, `usado_em` na 5.2).
  # ⚠️ **`criado_em` é escrito em Python, e a spec não previa isso.** O `now()`
  # do Postgres é o instante de **início da transação**: as linhas de uma mesma
  # transação saíam com carimbo idêntico e a trilha ficava sem ordem — o teste
  # dos quatro vereditos foi quem revelou. Diverge do `usuario.criado_em` de
  # propósito, e usa o mesmo relógio que grava `usado_em`.
  # ⚠️ **`EVENTO_ERRADO` grava no evento do caminho, não no do ingresso.** É a
  # contagem *deste* turno; gravar no do ingresso vazaria, pelo contador, a
  # existência de um ingresso de outro show para a portaria que o AD-7 proíbe de
  # saber disso.
  # ⚠️ **Os dois `INVALIDO` passaram a tocar o banco**, e o docstring do `validar`
  # se gabava de não tocar. Corrigido no mesmo commit — e o nome do teste
  # `..._sem_tocar_no_banco` também, achado do review adversarial.
  # ⚠️ **A tabela `validacao` quebrou a limpeza do teste de concorrência da 5.2**:
  # ela referencia `ingresso`, e o `DELETE` do `finally` passou a estourar com
  # `ForeignKeyViolation`, abortando a limpeza inteira e deixando um evento no
  # `rockhub_teste` que derrubou **33 testes** de programação, seed e reserva —
  # nenhum deles apontando para a portaria. Resolvido com o `DELETE` da
  # `validacao` antes.
  # ⚠️ **O contador fica fora da região `aria-live="assertive"`.** Dentro dela,
  # cada validação faria o leitor de tela anunciar o dado operacional atropelando
  # o veredito, que é a única coisa que precisa ser ouvida com a fila andando.
  # ⚠️ **Três formas até acertar o lugar do contador** (ajuste do Igor, na tela).
  # `ENTRADAS 41` em mono grande dentro do cabeçalho virava o segundo maior
  # elemento da tela e disputava leitura com o nome do show; encolhido, sumia.
  # Enquanto morasse **dentro** do bloco do título, todo tamanho era escolher
  # entre sumir e competir. Ficou em **faixa própria de quatro colunas** entre o
  # fio do cabeçalho e o botão da câmera, com hierarquia por cor (`--cal` nas
  # entradas, `--fumaca` nas três recusas) em vez de por corpo. O
  # `PainelDoTurno` perdeu o `children` e o CSS module inteiro nessa mudança.
  # ⚠️ **`entradas` entrou no `TurnoDaPortaria`, que duas telas leem.** O cartão
  # de `/portaria` desenha o número nos turnos abertos — por escolha, não por
  # esquecimento. As três recusas ficam só no `TurnoDoLeitor`.
  5-6-acompanhar-as-entradas-do-turno: done
  # ⚠️ Fica 'in-progress' até o merge da branch na main, como as epics 1 a 4.
  # O `bmad-code-review` da epic inteira ainda não rodou.
  epic-5-retrospective: optional

  # ---------- Epic 6: Avaliação sem atrito ----------
  epic-6: backlog
  6-1-readme-da-raiz-com-o-historico-de-decisoes: backlog
  6-2-conferir-o-passo-a-passo-numa-maquina-limpa: backlog
  6-3-documentar-o-uso-de-ia: backlog
  6-4-roteiro-de-avaliacao-de-ponta-a-ponta: backlog
  epic-6-retrospective: optional
